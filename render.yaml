# Run as FastAPI (uvicorn). Use $PORT so the platform can bind correctly.

services:
  - type: web
    name: tmkoc-yt
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
